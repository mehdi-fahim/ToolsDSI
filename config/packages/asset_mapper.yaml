framework:
    asset_mapper:
        paths:
            - assets/
            - vendor/symfony/stimulus-bundle/assets/
        missing_import_mode: strict

when@prod:
    framework:
        asset_mapper:
            missing_import_mode: warn
